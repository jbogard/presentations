namespace OrderProcessor
{
    // NOTE: You can use the "Rename" command on the "Refactor" menu to change the class name "ShippingService" in code, svc and config file together.
    public class ShippingService : IShippingService
    {
        public bool ProcessOrderForShipping(int orderId)
        {
            return true;
        }
    }
}
